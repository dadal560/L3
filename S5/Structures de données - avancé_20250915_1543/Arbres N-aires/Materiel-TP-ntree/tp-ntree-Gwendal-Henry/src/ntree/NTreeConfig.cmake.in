# see https://cmake.org/cmake/help/latest/module/CMakePackageConfigHelpers.html

@PACKAGE_INIT@

set_and_check(NTREE_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")
set_and_check(NTREE_LIB_DIRS "${PACKAGE_PREFIX_DIR}/lib")
set(TREE_LIBRARIES ntree)

check_required_components(Tree)
